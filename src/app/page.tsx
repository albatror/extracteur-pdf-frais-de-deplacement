import { ExpensePdfProcessor } from '@/components/ExpensePdfProcessor';

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-50">
      <ExpensePdfProcessor />
    </main>
  );
}
